---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: home-assistant
  namespace: home
spec:
  parentRefs:
    - name: cilium-gateway-local
      namespace: network
      sectionName: https-ha
  hostnames:
    - ha.stevevaradi.me
  rules:
    - backendRefs:
        - name: home-assistant
          port: 8080
