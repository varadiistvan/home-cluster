apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- helmrelease-jellyseerr.yaml
- helmrelease.yaml
configMapGenerator:
- name: jellyseer-values
  namespace: media
  files:
  - values.yaml=jellyseer-values.yaml
- name: jellyfin-values
  namespace: media
  files:
  - values.yaml=jellyfin-values.yaml
configurations:
- ../../common/kustomizeconfig.yaml
