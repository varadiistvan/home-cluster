apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: navidrome
  namespace: media
spec:
  parentRefs:
    - name: cilium-gateway-local
      namespace: network
      sectionName: https-navidrome
  hostnames:
    - navidrome.stevevaradi.me
  rules:
    - backendRefs:
        - name: navidrome
          port: 4533
