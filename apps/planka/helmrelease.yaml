---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: planka
  namespace: planka
spec:
  interval: 30m
  timeout: 10m
  install:
    remediation:
      retries: -1
  upgrade:
    remediation:
      retries: 3
  chart:
    spec:
      chart: planka
      # renovate: datasource=helm depName=planka registryUrl=https://plankanban.github.io/planka
      version: 2.2.x
      sourceRef:
        kind: HelmRepository
        name: planka
        namespace: flux-system
  valuesFrom:
  - kind: ConfigMap
    name: planka-values
