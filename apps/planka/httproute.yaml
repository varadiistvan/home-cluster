---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: planka
  namespace: planka
spec:
  parentRefs:
    - name: cilium-gateway-local
      namespace: network
      sectionName: https-planka
  hostnames:
    - planka.stevevaradi.me
  rules:
    - backendRefs:
        - name: planka
          port: 1337
