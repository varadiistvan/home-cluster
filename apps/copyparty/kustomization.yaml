apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- copyparty-config-secret.enc.yaml
- helmrelease.yaml
configMapGenerator:
- name: copyparty-values
  namespace: copyparty
  files:
  - values.yaml=copyparty-values.yaml
configurations:
- ../../common/kustomizeconfig.yaml
