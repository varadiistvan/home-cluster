apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- helmrelease.yaml
- httproute.yaml
- obico-smtp-secret.enc.yaml
configMapGenerator:
- name: obico-values
  namespace: home
  files:
  - values.yaml=obico-values.yaml
configurations:
- ../../common/kustomizeconfig.yaml
