---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: bytestash
  namespace: bytestash
spec:
  parentRefs:
    - name: cilium-gateway-local
      namespace: network
      sectionName: https-bytestash
  hostnames:
    - bytestash.stevevaradi.me
  rules:
    - backendRefs:
        - name: bytestash
          port: 5000
