---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: penpot
  namespace: penpot
spec:
  parentRefs:
    - name: cilium-gateway-local
      namespace: network
      sectionName: https-penpot
  hostnames:
    - penpot.stevevaradi.me
  rules:
    - backendRefs:
        - name: penpot
          port: 8080
