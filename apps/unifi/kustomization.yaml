apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- helmrelease.yaml
- ip-pool.yaml
- mongodb.yaml
- unifi-mongodb-secret.enc.yaml
configMapGenerator:
- name: unifi-values
  namespace: home
  files:
  - values.yaml=unifi-values.yaml
configurations:
- ../../common/kustomizeconfig.yaml
