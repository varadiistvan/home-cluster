---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: siyuan
  namespace: siyuan
spec:
  interval: 30m
  timeout: 10m
  install:
    remediation:
      retries: -1
  upgrade:
    remediation:
      retries: 3
  chart:
    spec:
      chart: siyuan
      version: 0.1.1
      sourceRef:
        kind: HelmRepository
        name: ex-trim
        namespace: flux-system
  valuesFrom:
  - kind: ConfigMap
    name: siyuan-values
