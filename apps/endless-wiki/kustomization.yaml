apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- helmrelease.yaml
- httproute.yaml
configMapGenerator:
- name: wiki-values
  namespace: scratch
  files:
  - values.yaml=wiki-values.yaml
configurations:
- ../../common/kustomizeconfig.yaml
