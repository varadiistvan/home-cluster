apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- convertx-jwt-secret.enc.yaml
- helmrelease.yaml
configMapGenerator:
- name: convertx-values
  namespace: convertx
  files:
  - values.yaml=convertx-values.yaml
configurations:
- ../../common/kustomizeconfig.yaml
