apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- helmrelease.yaml
- httproute.yaml
- ip-pool.yaml
configMapGenerator:
- name: syncthing-values
  namespace: syncthing
  files:
  - values.yaml=syncthing-values.yaml
configurations:
- ../../common/kustomizeconfig.yaml
