name: overleaf
apiVersion: v2

# Version of the chart (required)
version: 0.1.8

# Version of the application (required).
# This should be the main application version.
# renovate: datasource=docker depName=harbor.stevevaradi.me/stevevaradi/overleaf-kubernetes
appVersion: v5.5
description: A Helm chart for overleaf
