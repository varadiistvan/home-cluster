---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: nfd
  namespace: system
spec:
  interval: 30m
  timeout: 10m
  install:
    remediation:
      retries: -1
  upgrade:
    remediation:
      retries: 3
  chart:
    spec:
      chart: node-feature-discovery
      # renovate: datasource=docker depName=registry.k8s.io/nfd/charts/node-feature-discovery
      version: 0.18.3
      sourceRef:
        kind: HelmRepository
        name: nfd
        namespace: flux-system
  valuesFrom:
  - kind: ConfigMap
    name: nfd-values
