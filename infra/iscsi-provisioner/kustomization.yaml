apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- helmrelease.yaml
- iscsi-token-secret.enc.yaml
- registry-pass-secret.enc.yaml
configMapGenerator:
- name: iscsi-provisioner-values
  namespace: storage
  files:
  - values.yaml=iscsi-provisioner-values.yaml
configurations:
- ../../common/kustomizeconfig.yaml
