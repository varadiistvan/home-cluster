apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./cloudflare-api-key-secret.enc.yaml
  - ./helmrelease.yaml
configMapGenerator:
- name: cert-manager-values
  namespace: system
  files:
  - values.yaml=cert-manager-values.yaml
configurations:
- ../../common/kustomizeconfig.yaml
