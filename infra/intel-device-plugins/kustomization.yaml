apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- helmrelease.yaml
configMapGenerator:
- name: intel-device-operator-values
  namespace: system
  files:
  - values.yaml=intel-device-operator-values.yaml
configurations:
- ../../common/kustomizeconfig.yaml
