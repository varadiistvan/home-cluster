apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./helmrelease.yaml
configMapGenerator:
- name: tuppr-values
  namespace: system-upgrade
  files:
  - values.yaml=tuppr-values.yaml
configurations:
- ../../common/kustomizeconfig.yaml
