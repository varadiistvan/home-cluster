apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./helmrelease.yaml
configMapGenerator:
- name: misty-show-values
  namespace: monitoring
  files:
  - values.yaml=misty-show-values.yaml
configurations:
- ../../common/kustomizeconfig.yaml
