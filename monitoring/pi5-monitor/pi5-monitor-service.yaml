apiVersion: v1
kind: Service
metadata:
  name: pi5-monitor
  namespace: monitoring
  labels:
    app.kubernetes.io/name: pi5-monitor
    app.kubernetes.io/instance: pi5-monitor
spec:
  type: ClusterIP
  ports:
    - port: 9101
      targetPort: 9101
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: pi5-monitor
    app.kubernetes.io/instance: pi5-monitor
